const LearningBlocksStyle = ({colors, fonts}) => {
    return {
        div : {
            display: 'flex',
            'flex-wrap': 'wrap',
            'justify-content': 'center'
        }
    };
}

export default LearningBlocksStyle;